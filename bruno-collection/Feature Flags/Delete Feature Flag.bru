meta {
  name: Delete Feature Flag
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/feature-flags/:id
  body: none
  auth: none
}

params:path {
  id: {{featureFlagId}}
}

docs {
  Delete a feature flag by ID
}
